#include <stdio.h>
/**
 *main - entry point
 *
 *Description:prints alphabet in reverse
 *
 *Return: Always zero
 */
int main(void)
{
	char ch;
	for (ch = 'z';ch => 'a';ch--)
	{
		putchar(ch);
	}
	putchar(10);
	return (0);
}
