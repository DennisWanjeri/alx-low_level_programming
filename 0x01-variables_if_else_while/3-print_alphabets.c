#include <stdio.h>
/**
 *main - entry point
 *
 *Return: Always 0 (successful)
 *
 *Description: prints the alphabet in uppercase and lowercase
 */
int main()
{
	for (ch = 'A'; ch <= 'Z'; ch++;)
	{
		putchar(char);
			}
	for (ch = 'a';ch <= 'z';ch++;)
	{
		putchar(char);
	}
	putchar(10);
	return (0);
}
